<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="@color/colorPrimary">

    <EditText
        android:id="@+id/edit_slideTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:inputType="text"
        android:hint="Title"
        android:layout_alignParentTop="true"
        android:textAppearance="?attr/textAppearanceListItem"
        android:textColor="@color/dimmedWhite"
        android:textColorHint="@color/colorAccent"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_marginTop="64dp" />

    <NumberPicker
        android:id="@+id/edit_pickHour"
        android:layout_width="wrap_content"
        android:layout_height="60dp"
        android:layout_below="@id/edit_slideTitle"
        android:solidColor="@color/colorPrimary"
        android:background="@color/colorAccent" />

    <NumberPicker
        android:id="@+id/edit_pickMin"
        android:layout_toRightOf="@+id/edit_pickHour"
        android:layout_width="wrap_content"
        android:layout_height="60dp"
        android:layout_below="@id/edit_slideTitle"
        android:solidColor="@color/colorPrimary"
        android:background="@color/colorAccent" />

    <NumberPicker
        android:id="@+id/edit_pickSec"
        android:layout_toRightOf="@+id/edit_pickMin"
        android:layout_width="wrap_content"
        android:layout_height="60dp"
        android:layout_below="@id/edit_slideTitle"
        android:solidColor="@color/colorPrimary"
        android:background="@color/colorAccent" />
</RelativeLayout>
